# Functions in python -local variables

# local variable can't use as global variable without initialize if the variable is already used previously it can use that value
def display():
    a=100
    print(a)
display()
    
